# ============================================================================
# OpenSERGE Requirements
# ============================================================================
#
# Core dependencies for road graph extraction from satellite imagery
# For APLS metric, you'll also need Go 1.21+ (see metrics/apls/README.md)
#

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0
timm>=1.0.0

# Computer Vision
opencv-python>=4.8.0
pillow>=10.0.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
networkx>=3.1

# Progress & Utilities
tqdm>=4.65.0

# TOPO Metric Dependencies
hopcroftkarp>=1.2.5
rtree>=1.0.0
svgwrite>=1.4.0

# Experiment Tracking (Optional)
tensorboard>=2.14.0
wandb>=0.15.0

# Development & Notebooks (Optional)
jupyter>=1.0.0
jupyterlab>=4.0.0
notebook>=7.0.0
matplotlib>=3.7.0
ipykernel>=6.25.0

# ============================================================================
# Installation Instructions
# ============================================================================
#
# 1. Basic installation (CPU):
#    pip install -r requirements.txt
#
# 2. With CUDA support (recommended for training):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements.txt
#
# 3. Apple Silicon (MPS):
#    pip install -r requirements.txt
#    (PyTorch with MPS support is included by default in recent versions)
#
# 4. For APLS metric, install Go:
#    - Ubuntu: sudo apt install golang-go
#    - macOS: brew install go
#    - Windows: Download from https://go.dev/dl/
#
# 5. Verify installation:
#    python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available__()}')"
#    python -c "import timm; import cv2; import wandb; print('All packages OK')"
#
